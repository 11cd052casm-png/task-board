# CLAUDE.md

このファイルは、このリポジトリでコードを扱う際に Claude Code (claude.ai/code) が従うべきガイダンスです。

## プロジェクト概要

task-board プロジェクト。

*(プロジェクトの詳細な構成・技術スタック・アーキテクチャは、実装が進み次第このセクションに追記してください。)*

## Git 運用ルール

- **コードに変更を加えるたびに、コミットして GitHub にプッシュすること。** 変更を溜め込まず、1つのまとまった変更が完了した時点でこまめにコミット・プッシュする。
- コミットメッセージは変更内容が分かるように簡潔に書く(「何を」ではなく「なぜ」変更したかが分かるとなお良い)。
- push 前に `git status` / `git diff` で差分を確認し、意図しないファイル(認証情報や一時ファイルなど)が含まれていないか確認する。
- force push (`git push --force`)、`git reset --hard`、コミットの書き換え (`--amend` の乱用) など、履歴を破壊する操作は行わない。必要な場合は必ず事前にユーザーに確認する。
- pre-commit hook 等のチェックに引っかかった場合は `--no-verify` で回避せず、原因を修正してから再コミットする。
- 意味のあるまとまりごとにコミットを分け、1コミットに無関係な変更を混在させない。

## 開発コマンド

*(ビルド・テスト・Lint などのコマンドが定まり次第、このセクションに追記してください。)*
